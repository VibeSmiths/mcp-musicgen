name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  docker-nvidia:
    uses: Mossworks-Labs/.github/.github/workflows/docker-build.yml@main
    secrets: inherit
    with:
      image-name: mcp-musicgen

  # ROCm builds are heavy — uncomment when needed
  # docker-rocm:
  #   uses: Mossworks-Labs/.github/.github/workflows/docker-build.yml@main
  #   secrets: inherit
  #   with:
  #     image-name: mcp-musicgen-rocm
  #     dockerfile: Dockerfile.rocm
